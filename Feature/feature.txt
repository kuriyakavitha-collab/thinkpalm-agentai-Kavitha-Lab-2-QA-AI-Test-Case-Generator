Feature User Login Functionality

The application allows users to log in using their email and password.

Requirements
User can enter a valid email and password
Clicking “Login” should redirect to the dashboard
Invalid credentials should show an error message
Empty fields should trigger validation messages
Password field should be masked